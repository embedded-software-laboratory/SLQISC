def main()
  [var cur, var isP]
{
  cur := 2;
  call decPrint(cur) -> isP
}

def decPrint(var x)
  []
{
  out + / x 10000 28;
  out + % / x 1000 10 28;
  out + % / x 100 10 28;
  out + % / x 10 10 28;
  out + % x 10 28;
  trap
}
